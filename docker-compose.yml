version: '3.8'

services:
  app1:
    build: .
    container_name: app1_xss
    environment:
      - FLASK_APP=app1.py
      - FLASK_ENV=development
    ports:
      - "8001:5000"
    networks:
      - xss-lab

networks:
  xss-lab:
    driver: bridge
